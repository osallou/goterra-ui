redis:
        host: "localhost"
        port: 6379
        prefix: "goterra"

mongo:
        url: "mongodb://localhost:27017"
        db: "goterradeploy"


url: "http://localhost:8001"

fernet:
        - "0123456789"
web:
        listen: "0.0.0.0"
        port: 8004

deploy:
        # path where terraform scripts are written
        path: "/tmp"

amqp: "amqp://guest:guest@localhost:5672/"
